<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:HashForge"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Hash Forge" Height="700" Width="1120"
        MinWidth="900" MinHeight="600"
        WindowStyle="None" Background="{DynamicResource BackgroundBrush}"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" ResizeBorderThickness="8" GlassFrameThickness="1" CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
         <!-- Risorse Colore Tema - Dinamiche -->
         <SolidColorBrush x:Key="BackgroundBrush" Color="#050b16"/>
         <SolidColorBrush x:Key="CardBrush" Color="#111a2b"/>
         <SolidColorBrush x:Key="TextBrush" Color="#f8fafc"/>
         <SolidColorBrush x:Key="SecondaryTextBrush" Color="#94a3b8"/>
         <SolidColorBrush x:Key="PrimaryBrush" Color="#00c2ff"/>
         <SolidColorBrush x:Key="AccentBrush" Color="#34e0a1"/>
         <SolidColorBrush x:Key="DangerBrush" Color="#ff7b00"/>
         <SolidColorBrush x:Key="ControlBackgroundBrush" Color="#1e293b"/>
         <SolidColorBrush x:Key="BorderBrush" Color="#33ffffff"/>
         <SolidColorBrush x:Key="HoverBrush" Color="#26ffffff"/>
         <SolidColorBrush x:Key="ButtonForegroundBrush" Color="#0f172a"/>
         <SolidColorBrush x:Key="WindowControlsBrush" Color="#cbd5e1"/>
         <SolidColorBrush x:Key="HeaderTextBrush" Color="#ffffff"/>
         
         <Style TargetType="Button">
            <Setter Property="Background" Value="#1e293b"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
         </Style>

         <!-- Algorithm Toggle Button Style (Pill) -->
         <Style x:Key="AlgoToggleStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="1"
                                BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
         </Style>

         <!-- ComboBox Item Style -->
         <Style TargetType="ComboBoxItem">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderThickness="0,0,0,0" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="Indicator" Background="Transparent" CornerRadius="2" Margin="0,2,0,2"/>
                                <ContentPresenter Grid.Column="1" Margin="8,0,0,0" 
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                <Setter TargetName="Indicator" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
         </Style>

         <!-- ComboBox Style -->
         <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          Focusable="false" 
                                          IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" 
                                          ClickMode="Press"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" 
                                                CornerRadius="6"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="24"/>
                                                </Grid.ColumnDefinitions>
                                                <!-- Arrow -->
                                                <Path x:Name="Arrow" 
                                                      Grid.Column="1" 
                                                      Fill="#94a3b8"
                                                      HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center" 
                                                      Data="M0,0 L4,4 L8,0"
                                                      Stroke="#94a3b8"
                                                      StrokeThickness="1.5"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                                <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource PrimaryBrush}"/>
                                                <Setter TargetName="Arrow" Property="Stroke" Value="{DynamicResource PrimaryBrush}"/>
                                                <Setter Property="Cursor" Value="Hand"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="true">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            
                            <ContentPresenter Name="ContentSite" 
                                              IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="12,0,24,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              TextElement.Foreground="{DynamicResource TextBrush}"/>
                            
                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" 
                                   Focusable="False"
                                   PopupAnimation="Slide"
                                   VerticalOffset="4">
                                <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="{DynamicResource ControlBackgroundBrush}"
                                            BorderThickness="1"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            CornerRadius="6"
                                            Margin="0,0,8,8">
                                        <Border.Effect>
                                            <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.4" ShadowDepth="4"/>
                                        </Border.Effect>
                                        <ScrollViewer Margin="0,4" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
         </Style>
         
         <!-- Modern ScrollBar Style -->
         <Style TargetType="ScrollBar">
             <Setter Property="Background" Value="Transparent"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="ScrollBar">
                         <Grid>
                             <!-- Vertical ScrollBar -->
                             <Grid x:Name="VerticalRoot" Visibility="Collapsed">
                                 <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                 </Grid.RowDefinitions>
                                 
                                 <!-- Up Button (hidden) -->
                                 <RepeatButton Grid.Row="0" Height="0" Command="ScrollBar.LineUpCommand" Opacity="0"/>
                                 
                                 <!-- Track -->
                                 <Track Grid.Row="1" x:Name="PART_Track" IsDirectionReversed="True" Width="8" Margin="0,4">
                                     <Track.Thumb>
                                         <Thumb>
                                             <Thumb.Template>
                                                 <ControlTemplate>
                                                     <Border Background="{DynamicResource ControlBackgroundBrush}" CornerRadius="4">
                                                         <Border.Style>
                                                             <Style TargetType="Border">
                                                                 <Style.Triggers>
                                                                     <Trigger Property="IsMouseOver" Value="True">
                                                                         <Setter Property="Background" Value="#475569"/>
                                                                     </Trigger>
                                                                 </Style.Triggers>
                                                             </Style>
                                                         </Border.Style>
                                                     </Border>
                                                 </ControlTemplate>
                                             </Thumb.Template>
                                         </Thumb>
                                     </Track.Thumb>
                                 </Track>
                                 
                                 <!-- Down Button (hidden) -->
                                 <RepeatButton Grid.Row="2" Height="0" Command="ScrollBar.LineDownCommand" Opacity="0"/>
                             </Grid>
                             
                             <!-- Horizontal ScrollBar (completely hidden) -->
                             <Grid x:Name="HorizontalRoot" Visibility="Collapsed" Height="0" Opacity="0"/>
                         </Grid>
                         
                         <ControlTemplate.Triggers>
                             <Trigger Property="Orientation" Value="Vertical">
                                 <Setter TargetName="VerticalRoot" Property="Visibility" Value="Visible"/>
                             </Trigger>
                             <Trigger Property="Orientation" Value="Horizontal">
                                 <Setter TargetName="HorizontalRoot" Property="Visibility" Value="Collapsed"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>
         
         <!-- ListBox Style - Disable Horizontal Scroll -->
         <Style TargetType="ListBox">
             <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
             <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
             <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
             <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel"/>
         </Style>

         <!-- Context Menu -->
         <ContextMenu x:Key="RowContextMenu">
             <MenuItem Header="Copia Hash" x:Name="CtxCopyHash"/>
             <MenuItem Header="Copia Percorso" x:Name="CtxCopyPath"/>
             <Separator/>
             <MenuItem Header="Apri Cartella" x:Name="CtxOpenFolder"/>
         </ContextMenu>
         <!-- Window Control Button Styles -->
         <Style x:Key="WindowButtonStyle" TargetType="Button">
             <Setter Property="Background" Value="Transparent"/>
             <Setter Property="Foreground" Value="{DynamicResource WindowControlsBrush}"/>
             <Setter Property="Width" Value="46"/>
             <Setter Property="Height" Value="32"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="Button">
                         <Border Background="{TemplateBinding Background}">
                             <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                         </Border>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
             <Style.Triggers>
                 <Trigger Property="IsMouseOver" Value="True">
                     <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                     <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                 </Trigger>
             </Style.Triggers>
         </Style>

         <Style x:Key="WindowCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowButtonStyle}">
             <Setter Property="Foreground" Value="#ff6b6b"/>
             <Style.Triggers>
                 <Trigger Property="IsMouseOver" Value="True">
                     <Setter Property="Background" Value="#e81123"/>
                     <Setter Property="Foreground" Value="White"/>
                 </Trigger>
             </Style.Triggers>
         </Style>
    </Window.Resources>

    <Border x:Name="MainBorder" Background="{DynamicResource BackgroundBrush}">
        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource AncestorType=Window}}" Value="Maximized">
                        <Setter Property="Margin" Value="8,8,8,60"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/> <!-- Title Bar -->
                <RowDefinition Height="Auto"/> <!-- Compact Header -->
                <RowDefinition Height="*"/>  <!-- Content -->
                <RowDefinition Height="32"/> <!-- Status Bar -->
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" Margin="16,0">
                    <!-- Logo removed from title bar as requested -->
                    <TextBlock Text="Hash Forge" Foreground="{DynamicResource TextBrush}" 
                               VerticalAlignment="Center" Margin="0,0,0,0" FontSize="12" FontWeight="SemiBold" Opacity="0.7"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                    <!-- Pulsante Cambio Tema -->
                    <Button x:Name="ThemeBtn" Style="{StaticResource WindowButtonStyle}" ToolTip="Cambia Tema">
                        <Path x:Name="ThemeIcon" Data="M17.75,4.09L15.22,6.03L16.13,9.09L13.5,7.28L10.87,9.09L11.78,6.03L9.25,4.09L12.44,4L13.5,1L14.56,4L17.75,4.09M21.25,11L19.61,12.25L20.2,14.23L18.5,13.06L16.8,14.23L17.39,12.25L15.75,11L17.81,10.95L18.5,9L19.19,10.95L21.25,11M18.97,15.95C19.8,15.87 20.69,17.05 20.16,17.8C19.84,18.25 19.5,18.67 19.08,19.07C15.17,23 8.84,23 4.94,19.07C1.03,15.17 1.03,8.83 4.94,4.93C5.34,4.53 5.76,4.17 6.21,3.85C6.96,3.32 8.14,4.21 8.06,5.04C7.79,7.9 8.75,10.87 10.95,13.06C13.14,15.26 16.1,16.22 18.97,15.95M17.33,17.97C14.5,17.81 11.7,16.64 9.53,14.5C7.36,12.31 6.2,9.5 6.04,6.68C3.23,9.82 3.34,14.64 6.35,17.66C9.37,20.67 14.19,20.78 17.33,17.97Z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Stretch="Uniform" Width="14" Height="14" VerticalAlignment="Center" HorizontalAlignment="Center" SnapsToDevicePixels="True"/>
                    </Button>
                    <Button x:Name="SettingsBtn" Style="{StaticResource WindowButtonStyle}" ToolTip="Impostazioni">
                        <Path Data="M12,8A4,4 0 0,1 16,12A4,4 0 0,1 12,16A4,4 0 0,1 8,12A4,4 0 0,1 12,8M12,10A2,2 0 0,0 10,12A2,2 0 0,0 12,14A2,2 0 0,0 14,12A2,2 0 0,0 12,10M10,22C9.75,22 9.54,21.82 9.5,21.58L9.13,18.93C8.5,18.68 7.96,18.34 7.44,17.94L4.95,18.95C4.73,19.03 4.46,18.95 4.34,18.73L2.34,15.27C2.21,15.05 2.27,14.78 2.46,14.63L4.56,12.97L4.5,12L4.56,11.03L2.46,9.37C2.27,9.22 2.21,8.95 2.34,8.73L4.34,5.27C4.46,5.05 4.73,4.96 4.95,5.05L7.44,6.05C7.96,5.66 8.5,5.32 9.13,5.07L9.5,2.42C9.54,2.18 9.75,2 10,2H14C14.25,2 14.46,2.18 14.5,2.42L14.87,5.07C15.5,5.32 16.04,5.66 16.56,6.05L19.05,5.05C19.27,4.96 19.54,5.05 19.66,5.27L21.66,8.73C21.79,8.95 21.73,9.22 21.54,9.37L19.44,11.03L19.5,12L19.44,12.97L21.54,14.63C21.73,14.78 21.79,15.05 21.66,15.27L19.66,18.73C19.54,18.95 19.27,19.03 19.05,18.95L16.56,17.94C16.04,18.34 15.5,18.68 14.87,18.93L14.5,21.58C14.46,21.82 14.25,22 14,22H10Z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Stretch="Uniform" Width="14" Height="14" VerticalAlignment="Center" HorizontalAlignment="Center" SnapsToDevicePixels="True"/>
                    </Button>
                    <Button x:Name="MinimizeBtn" Style="{StaticResource WindowButtonStyle}">
                        <Rectangle Width="10" Height="1" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" VerticalAlignment="Center" HorizontalAlignment="Center" SnapsToDevicePixels="True"/>
                    </Button>
                    <Button x:Name="MaximizeBtn" Style="{StaticResource WindowButtonStyle}">
                        <Rectangle Width="10" Height="10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1" VerticalAlignment="Center" HorizontalAlignment="Center" SnapsToDevicePixels="True"/>
                    </Button>
                    <Button x:Name="CloseBtn" Style="{StaticResource WindowCloseButtonStyle}">
                        <Path Data="M 0,0 L 10,10 M 0,10 L 10,0" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.5" VerticalAlignment="Center" HorizontalAlignment="Center" SnapsToDevicePixels="True"/>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Compact Header -->
            <Border Grid.Row="1" Margin="24,0,24,16" CornerRadius="8" Height="60">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#2000c2ff" Offset="0"/>
                        <GradientStop Color="#101363ee" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel VerticalAlignment="Center" Margin="24,0" Orientation="Horizontal">
                        <!-- Logo Image - loaded programmatically from embedded resource -->
                        <Image x:Name="LogoImage" Width="32" Height="32" Margin="0,0,16,0" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality">
                            <Image.Effect>
                                <DropShadowEffect Color="#00c2ff" BlurRadius="10" Opacity="0.4" ShadowDepth="0"/>
                            </Image.Effect>
                        </Image>
                        <StackPanel VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Hash Forge" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource HeaderTextBrush}"/>
                                <TextBlock Text="v1.2.0" FontSize="12" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" Margin="8,0,0,0" FontWeight="SemiBold"/>
                            </StackPanel>
                            <TextBlock Text="Utility per calcolo e validazione di hash (MD5, SHA, ecc.)." Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <!-- Optional: Add some stats or info here if needed in future -->
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="2" Margin="24,0,24,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" MinWidth="300"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="2*" MinWidth="400"/>
                </Grid.ColumnDefinitions>

                <!-- Controls Card -->
                <Border Grid.Column="0" Background="{DynamicResource CardBrush}" CornerRadius="16" BorderBrush="#1affffff" BorderThickness="1" Padding="24">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/> <!-- Header -->
                            <RowDefinition Height="Auto"/> <!-- Combos -->
                            <RowDefinition Height="Auto"/> <!-- Buttons -->
                            <RowDefinition Height="*"/>    <!-- FileList (Flexible) -->
                            <RowDefinition Height="Auto"/> <!-- Compute -->
                            <RowDefinition Height="Auto"/> <!-- Progress -->
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="CONTROLLI" Foreground="#94a3b8" FontSize="12" FontWeight="Bold" Margin="0,0,0,16"/>
                        
                        <!-- Controls Row -->
                        <Grid Grid.Row="1" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Algorithm Selector Button with Badge -->
                            <Grid Grid.Column="0">
                                <Button x:Name="AlgoSelectorBtn" Height="34" Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource TextBrush}" FontSize="12" Content="⚙️ Algoritmi"/>
                                <!-- Notification Badge -->
                                <Border Background="#22c55e" CornerRadius="8" Padding="5,1" 
                                        HorizontalAlignment="Right" VerticalAlignment="Top" 
                                        Margin="0,-6,-6,0" MinWidth="16">
                                    <TextBlock x:Name="AlgoStatusText" Text="1" Foreground="White" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center"/>
                                </Border>
                            </Grid>
                            
                            <!-- Hash Verification Button -->
                            <Button x:Name="ManageHashesBtn" Grid.Column="2" Content="📋 Verifica Hash" Height="34" Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource TextBrush}" FontSize="12"/>
                        </Grid>
                        
                        <!-- Algorithm Selection Popup -->
                        <Popup x:Name="AlgoPopup" Placement="Bottom" PlacementTarget="{Binding ElementName=AlgoSelectorBtn}" StaysOpen="False" AllowsTransparency="True">
                            <Border Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,4,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.3" ShadowDepth="4"/>
                                </Border.Effect>
                                <StackPanel>
                                    <TextBlock Text="Seleziona Algoritmi" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="4,0,0,8"/>
                                    <StackPanel Orientation="Horizontal">
                                        <ToggleButton x:Name="ChkMD5" Content="MD5" Style="{StaticResource AlgoToggleStyle}" Margin="0,0,4,0"/>
                                        <ToggleButton x:Name="ChkSHA1" Content="SHA1" Style="{StaticResource AlgoToggleStyle}" Margin="0,0,4,0"/>
                                        <ToggleButton x:Name="ChkSHA256" Content="SHA256" Style="{StaticResource AlgoToggleStyle}" IsChecked="True" Margin="0,0,4,0"/>
                                        <ToggleButton x:Name="ChkSHA512" Content="SHA512" Style="{StaticResource AlgoToggleStyle}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </Popup>

                        <Grid Grid.Row="2" Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="AddFileBtn" Grid.Column="0" Content="📄 File" Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource ButtonForegroundBrush}" FontWeight="Bold" FontSize="13"/>
                            <Button x:Name="AddFolderBtn" Grid.Column="2" Content="📁 Cartella" Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource ButtonForegroundBrush}" FontWeight="Bold" FontSize="13"/>
                            <Button x:Name="ClearBtn" Grid.Column="4" Content="🗑️ Svuota" Background="{DynamicResource DangerBrush}" Foreground="{DynamicResource ButtonForegroundBrush}" FontWeight="Bold" FontSize="13"/>
                        </Grid>


                        <ListBox Grid.Row="3" x:Name="FileList" Background="{DynamicResource BackgroundBrush}" BorderThickness="0" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,16" AllowDrop="True">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Icon -->
                                        <TextBlock Grid.Column="0" Text="📄" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        
                                        <!-- File Name -->
                                        <TextBlock Grid.Column="1" Text="{Binding}" FontSize="13" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Padding" Value="8,6"/>
                                    <Setter Property="Margin" Value="0,1"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border Background="{TemplateBinding Background}" 
                                                        Padding="{TemplateBinding Padding}"
                                                        CornerRadius="4">
                                                    <ContentPresenter/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>


                        <Button Grid.Row="4" x:Name="ComputeBtn" Content="⚡ Calcola Hash" Background="{DynamicResource AccentBrush}" Foreground="{DynamicResource ButtonForegroundBrush}" FontWeight="Bold" Height="44" FontSize="15">
                            <Button.Effect>
                                <DropShadowEffect Color="#34e0a1" BlurRadius="20" Opacity="0.3" ShadowDepth="0"/>
                            </Button.Effect>
                        </Button>

                        <Button Grid.Row="4" x:Name="StopBtn" Content="⛔ Interrompi" Background="{DynamicResource DangerBrush}" Foreground="{DynamicResource ButtonForegroundBrush}" FontWeight="Bold" Height="44" FontSize="15" Visibility="Collapsed">
                            <Button.Effect>
                                <DropShadowEffect Color="#ff7b00" BlurRadius="20" Opacity="0.3" ShadowDepth="0"/>
                            </Button.Effect>
                        </Button>
                        
                        <StackPanel x:Name="StatsPanel" Grid.Row="5" Visibility="Collapsed">
                            <!-- Smart Stats Grid -->
                            <UniformGrid Rows="1" Columns="4" Margin="0,24,0,8">
                                <StackPanel Margin="0,0,8,0">
                                    <TextBlock Text="FILE" FontSize="10" Foreground="#64748b" FontWeight="Bold"/>
                                    <TextBlock x:Name="StatFiles" Text="-" FontSize="13" Foreground="#e2e8f0" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,8,0">
                                    <TextBlock Text="DATI" FontSize="10" Foreground="#64748b" FontWeight="Bold"/>
                                    <TextBlock x:Name="StatData" Text="-" FontSize="13" Foreground="#e2e8f0" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,8,0">
                                    <TextBlock Text="VELOCITÀ" FontSize="10" Foreground="#64748b" FontWeight="Bold"/>
                                    <TextBlock x:Name="StatSpeed" Text="-" FontSize="13" Foreground="#e2e8f0" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="STIMA" FontSize="10" Foreground="#64748b" FontWeight="Bold"/>
                                    <TextBlock x:Name="StatETA" Text="-" FontSize="13" Foreground="#e2e8f0" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </UniformGrid>

                            <!-- Pro Progress Bar -->
                            <Border Height="10" CornerRadius="5" Background="{DynamicResource ControlBackgroundBrush}" ClipToBounds="True">
                                <ProgressBar x:Name="Progress" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource PrimaryBrush}" Value="0" Maximum="100"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Results Card -->
                <Border Grid.Column="2" Background="{DynamicResource CardBrush}" CornerRadius="16" BorderBrush="#1affffff" BorderThickness="1" Padding="24">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBlock Text="RISULTATI" Foreground="#94a3b8" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                            <Button x:Name="CopyBtn" Content="📋 Copia" Margin="16,0,0,0" Padding="12,6" FontSize="12" Background="{DynamicResource HoverBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
                            <Button x:Name="ExportBtn" Content="💾 Esporta" Margin="8,0,0,0" Padding="12,6" FontSize="12" Background="{DynamicResource HoverBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
                        </StackPanel>

                        <DataGrid x:Name="ResultsGrid" Grid.Row="1" Background="Transparent" BorderThickness="0" 
                                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                                  AutoGenerateColumns="False" IsReadOnly="True"
                                  HorizontalGridLinesBrush="{DynamicResource BorderBrush}" RowBackground="Transparent"
                                  Foreground="{DynamicResource TextBrush}">
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                    <Setter Property="Padding" Value="8"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                </Style>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Padding" Value="8"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Padding="{TemplateBinding Padding}" Background="Transparent" SnapsToDevicePixels="True">
                                                    <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Margin" Value="0,1"/>
                                    <Setter Property="ContextMenu" Value="{StaticResource RowContextMenu}"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridRow">
                                                <Border x:Name="DGR_Border"
                                                        Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="4"
                                                        SnapsToDevicePixels="True">
                                                    <DataGridCellsPresenter ItemsPanel="{TemplateBinding ItemsPanel}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="DGR_Border" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="DGR_Border" Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="FILE" Binding="{Binding Name}" Width="2*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="ALGORITMO" Binding="{Binding Algorithm}" Width="Auto">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="HASH" Binding="{Binding Hash}" Width="3*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontFamily" Value="Consolas"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="DIMENSIONE" Binding="{Binding Size}" Width="Auto">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                
                                <!-- Status Column -->
                                <DataGridTemplateColumn x:Name="StatusColumn" Header="STATUS" Width="Auto" Visibility="Collapsed">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock FontWeight="Bold" HorizontalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value=""/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding MatchStatus}" Value="True">
                                                                <Setter Property="Text" Value="✅ OK"/>
                                                                <Setter Property="Foreground" Value="#4ade80"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding MatchStatus}" Value="False">
                                                                <Setter Property="Text" Value="❌ NO"/>
                                                                <Setter Property="Foreground" Value="#f87171"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>

            <!-- Status Bar -->
            <Border Grid.Row="3" Background="{DynamicResource BackgroundBrush}" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderBrush}">
                <Grid Margin="16,0">
                    <TextBlock x:Name="StatusBarText" Text="Pronto" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" FontSize="12" HorizontalAlignment="Left"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="William Tritapepe" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Opacity="0.7"/>
                        <TextBlock Text=" • " Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Opacity="0.5"/>
                        <TextBlock x:Name="GitHubLink" Text="GitHub" Foreground="{DynamicResource PrimaryBrush}" FontSize="11" Cursor="Hand" TextDecorations="Underline"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Settings Modal Overlay -->
            <Grid x:Name="SettingsOverlay" Grid.RowSpan="4" Background="#AA000000" Visibility="Collapsed" Panel.ZIndex="1000">
                <Border Width="400" Height="350" Background="{DynamicResource CardBrush}" CornerRadius="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Grid Margin="24">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Header -->
                        <TextBlock Text="Impostazioni" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource HeaderTextBrush}" Margin="0,0,0,24"/>
                        
                        <!-- Content -->
                        <StackPanel Grid.Row="1">
                            <!-- Default Algo -->
                            <TextBlock Text="Algoritmo Predefinito" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,8"/>
                            <ComboBox x:Name="SettingsAlgoCombo" Padding="8" Margin="0,0,0,24">
                                <ComboBoxItem Content="MD5"/>
                                <ComboBoxItem Content="SHA1"/>
                                <ComboBoxItem Content="SHA256"/>
                                <ComboBoxItem Content="SHA512"/>
                            </ComboBox>

                            <!-- Shell Integration -->
                            <TextBlock Text="Integrazione Windows" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,8"/>
                            <TextBlock Text="Aggiungi 'Hash Forge' al menu tasto destro" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,12" TextWrapping="Wrap"/>
                            <Button x:Name="ToggleContextBtn" Content="Aggiungi al Menu Contestuale" Height="36" Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource TextBrush}" BorderThickness="0"/>
                            <TextBlock x:Name="ContextStatusText" Text="" Foreground="#4ade80" FontSize="11" Margin="0,8,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <!-- Footer -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                            <Button x:Name="CloseSettingsBtn" Content="Chiudi" Width="100" Height="36" Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource ButtonForegroundBrush}" FontWeight="Bold"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- Hash List Overlay -->
            <Grid x:Name="HashListOverlay" Grid.RowSpan="4" Background="#AA000000" Visibility="Collapsed" Panel.ZIndex="1000">
                <Border Width="500" Height="450" Background="{DynamicResource CardBrush}" CornerRadius="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Grid Margin="24">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Header -->
                        <StackPanel Grid.Row="0">
                            <TextBlock Text="Lista Hash Attesi" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource HeaderTextBrush}" Margin="0,0,0,8"/>
                            <TextBlock Text="Incolla qui gli hash (uno per riga) o carica un file di hash (.txt, .md5, .sha256...)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,16" TextWrapping="Wrap"/>
                        </StackPanel>
                        
                        <!-- Content -->
                        <TextBox x:Name="HashListInput" Grid.Row="1" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" 
                                 Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource TextBrush}" 
                                 BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="8" FontFamily="Consolas"/>

                        <!-- Footer -->
                        <Grid Grid.Row="2" Margin="0,16,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Orientation="Horizontal" Grid.Column="0">
                                <Button x:Name="LoadHashFileBtn" Content="📂 Carica File" Padding="12,8" Margin="0,0,8,0" Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource TextBrush}" BorderThickness="0"/>
                                <Button x:Name="ClearHashListBtn" Content="🗑️ Pulisci" Padding="12,8" Background="{DynamicResource DangerBrush}" Foreground="{DynamicResource ButtonForegroundBrush}" FontWeight="Bold" BorderThickness="0"/>
                            </StackPanel>
                            
                            <StackPanel Orientation="Horizontal" Grid.Column="2">
                                <Button x:Name="CancelHashListBtn" Content="Annulla" Padding="12,8" Margin="0,0,8,0" Background="Transparent" Foreground="{DynamicResource SecondaryTextBrush}" BorderThickness="0"/>
                                <Button x:Name="SaveHashListBtn" Content="Salva e Chiudi" Padding="16,8" Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource ButtonForegroundBrush}" FontWeight="Bold" BorderThickness="0"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
